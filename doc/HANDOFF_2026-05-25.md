# Handoff lighthouse-pdep — 2026-05-25

## 1. Estado del proyecto
Next 14 + Supabase. Backend financiero estable. UI /fondos, /gastos, /pagos al día. Base reseteada el 2026-05-24 — DB con datos de prueba mínimos.

## 2. Git
- Último commit: `45a82b3` UX-EXPORT-FONDOS
- Working tree: solo `src/lib/version.ts` modificado (regen automática de predev/prebuild — ignorar).
- Branch: `main`. Sincronizado con `origin/main`.

## 3. Migraciones SQL
**Aplicadas en Supabase** (todas las del repo + correcciones):
- 20260524000000..05: FIN2 (imputaciones, RPC v2, anular, view PG, fix saldo MP, gastos volver pendiente)
- 20260525000000: tipos_gasto
- 20260525000001: gastos.periodo_analitico **vía trigger** (la versión GENERATED falló)
- 20260525000002: ordenes_pago + OP-fix (nro_pago en snapshot)

**Pendientes:** ninguna crítica.

## 4. Funcionalidades cerradas hoy
- **FIN2/FIN2.x**: aportes con imputaciones múltiples (MP+Terceros), anular con reversa, view v_posicion_global_risa, detalle modal.
- **TIPOS-GASTO**: clasificación analítica con alta inline desde modal de gasto.
- **PG-PERIODO**: gastos.periodo_analitico (YYYY-MM) via trigger BEFORE INSERT/UPDATE.
- **OP**: ordenes_pago auto-generadas al confirmar pago, anuladas al anular. Numeración reset anual OP-YYYY-NNNNN. Vista imprimible `/ordenes-pago/[codigo]` con `window.print()`.
- **PAGOS sin borrador**: UI crea+confirma en un solo acto. DB conserva enum 'borrador' para compat.
- **UX Fondos**: RISA y Aportes colapsables; modales detalle Tercero/Aporte; export 5 hojas (Cuenta_RISA, Terceros_Resumen, Terceros_Movimientos, Aportes, Aporte_Imputaciones).
- **Misc**: formato es-AR en inputs de monto (lib/money.ts), columna OP 2da en pagos, clean concepto, FIN2.7b destino derivado.

## 5. Problemas abiertos
- Ninguno bloqueante.
- Lote E2E formal del handoff anterior (2026-05-24) **sigue pendiente** — se priorizó FIN2.7..UX-EXPORT en lugar de testing manual.
- Tareas legacy `#133/#134` (GastosClient extender FormState) probablemente superadas por TIPOS-GASTO/forma_cancelacion; verificar antes de retomar.

## 6. Próximo paso recomendado
Una de tres opciones:
1. **Lote E2E formal** (gasto MP/Tercero → pago → OP → aporte mixto → anular). Confirmaría que toda la cadena funciona end-to-end.
2. **Dashboard / Reportería** aprovechando `tipo_gasto_id` + `periodo_analitico` + `ordenes_pago` (todo listo para GROUP BY).
3. **Vercel deploy** + **Google Login** (cambio de frente, infra).

## 7. Archivos clave (cambios recientes)
- `src/app/(dashboard)/fondos/FondosClient.tsx` — UX detalles + export 5-hojas
- `src/app/(dashboard)/pagos/PagosClient.tsx` — sin borrador + UX-3 concepto + OP 2da col
- `src/app/(dashboard)/gastos/GastosClient.tsx` — TIPOS-GASTO + money format
- `src/app/(dashboard)/ordenes-pago/[codigo]/page.tsx` + PrintButton — vista imprimible OP
- `src/lib/money.ts` (nuevo) — parseMoneyInput/formatMoneyInput es-AR
- `src/lib/excel.ts` — helper `exportWorkbookToExcel` multi-hoja
- `supabase/migrations/2026052500000{0,1,2}_*.sql`

## 8. Levantar dev
```
Stop-Process -Name node -Force -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
npm run dev
```
Después: `http://localhost:3000`. Login con usuario configurado en `profiles`.

## 9. Qué NO tocar
- DataTable global (`src/components/DataTable.tsx`) — patrón es modal, no inline expand.
- Lógica financiera de movimientos_fondo / movimientos_financiacion.
- Ramas RISA/Tercero en `fn_confirmar_pago` / `fn_anular_pago` (preservadas verbatim del P4b vigente).
- Enum `pagos.estado='borrador'` — sigue en DB para compat, UI lo filtra con `.neq()`.
- RLS sin pedido explícito (`/diagnose-rls` primero).
- `npm run build` mientras `npm run dev` corre (corrompe `.next/`).
- Service_role en frontend.

## 10. Checklist mínima al retomar
1. `git status` → working tree limpio salvo version.ts.
2. `npx tsc --noEmit` → sin errores.
3. Levantar dev, entrar `/fondos` → ver MP+MT=RISA arriba, secciones colapsables.
4. `/pagos` → tabla pagos registrados con columna OP 2da, sin sección Borradores.
5. `/gastos` → modal con select Tipo de gasto + monto formato es-AR.
6. Click "Exportar Fondos" en `/fondos` → descarga `.xlsx` con 5 hojas.
7. Click "Ver OP" en una fila de pago → ruta `/ordenes-pago/[codigo]` imprimible.
